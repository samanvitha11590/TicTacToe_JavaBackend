package strategy;

import models.Board;
import models.Move;

public class MediumPlayingStrategy implements BotPlayingStrategy {
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}
